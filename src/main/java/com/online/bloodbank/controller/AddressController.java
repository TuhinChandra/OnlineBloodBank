package com.online.bloodbank.controller;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class AddressController {

	/*
	 * @Autowired private AddressService addressService;
	 *
	 * @RequestMapping(value = "/createAddress/{city}/{streetNo}/{pinCode}/{state}",
	 * method = RequestMethod.GET, produces = "application/json")
	 *
	 * @ResponseBody public Address createAddress(@PathVariable("city") final String
	 * city,
	 *
	 * @PathVariable("streetNo") final String streetNo, @PathVariable("pinCode")
	 * final long pinCode,
	 *
	 * @PathVariable("state") final String state) throws Exception { return
	 * addressService.register(city, streetNo, pinCode, state); }
	 */

}
